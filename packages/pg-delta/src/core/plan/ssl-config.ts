/**
 * Re-export from platform. The canonical implementation lives in
 * `platform/sql/ssl-config.ts` — this barrel preserves existing imports.
 */
export { parseSslConfig } from "../../platform/sql/ssl-config.ts";
