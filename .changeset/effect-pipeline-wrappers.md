---
"@supabase/pg-delta": minor
---

Add Effect-native wrappers for high-level pipeline functions: `createPlanEffect`, `applyPlanEffect`, `applyDeclarativeSchemaEffect`, and `loadDeclarativeSchemaEffect`. Also export all error classes and database service types from the package entry point.
